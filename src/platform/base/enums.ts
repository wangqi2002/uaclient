export enum Sources {
    database = 'PlatForm.DataBase',
}

export enum Errors {
    dbError = 'Exception writing to database',
}

export enum Warns {
    noModel = 'The model you trying to get is undefined',
}

export enum Infos {

}